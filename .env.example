# ==========================================
# 🔐 SECRETS (Fill these in!)
# ==========================================
UPSTOX_ACCESS_TOKEN=your_access_token_here
UPSTOX_REFRESH_TOKEN=your_refresh_token_here
UPSTOX_CLIENT_ID=your_client_id
UPSTOX_CLIENT_SECRET=your_client_secret
UPSTOX_REDIRECT_URI=http://localhost:8000/callback

# Upstox API Base URLs (Required by MarketDataClient)
UPSTOX_BASE_V2=https://api.upstox.com/v2
UPSTOX_BASE_V3=https://api.upstox.com/v3

# Admin Key for Manual Overrides (Emergency Stop)
ADMIN_SECRET=change_this_to_a_complex_password

# ==========================================
# ⚙️ SYSTEM CONFIG
# ==========================================
# Environment: 'shadow', 'production_semi', or 'production_live'
ENVIRONMENT=shadow
PROJECT_NAME="VolGuard Algorithmic Trading"
VERSION="3.1.0"
DEBUG=False

# ==========================================
# 🗄️ INFRASTRUCTURE (The New Plumbing)
# ==========================================
# Redis is now REQUIRED for the Executor & Idempotency
# Docker: Use hostname 'redis' | Local testing: Use 'localhost'
REDIS_URL=redis://redis:6379/0

# Postgres Database
POSTGRES_SERVER=postgres
POSTGRES_USER=volguard
POSTGRES_PASSWORD=volguard_secure
POSTGRES_DB=volguard_production
# Calculated URL: postgresql+asyncpg://volguard:volguard_secure@postgres/volguard_production

# Timezone (Critical for Algo)
TZ=Asia/Kolkata

# ==========================================
# 💰 RISK & TRADING LIMITS
# ==========================================
BASE_CAPITAL=1000000.0
MAX_DAILY_LOSS=20000.0
MAX_PORTFOLIO_LOSS=50000.0
MAX_POSITIONS=6

# Risk Per Trade
MAX_NET_DELTA=0.40
MAX_GAMMA=0.15
MAX_VEGA=1000.0

# Supervisor Speed (Seconds)
SUPERVISOR_LOOP_INTERVAL=3.0
SUPERVISOR_WEBSOCKET_ENABLED=true

# ==========================================
# 🔔 ALERTS
# ==========================================
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
SLACK_WEBHOOK_URL=
